{
  "name": "ashhad-portfolio",
  "version": "1.0.0",
  "description": "Modern MERN stack developer portfolio for Ash Had P A",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:vercel": "npm install && cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd client && npm install --include=dev && cd ../server && npm install",
    "vercel-build": "cd client && npm install --include=dev && npm run build"
  },
  "keywords": ["portfolio", "mern", "react", "nodejs", "mongodb"],
  "author": "Ash Had P A",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
